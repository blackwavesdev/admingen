{
  "name": "admingen-framework",
  "private": true,
  "workspaces": [
    "packages/*",
    "example-app"
  ],
  "scripts": {
"build": "cd packages/admingen && bun run build"
},
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}