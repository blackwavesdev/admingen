{
  "name": "example-app",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "db:generate": "bunx drizzle-kit generate",
    "db:migrate": "bun run src/migrate.ts",
    "db:seed": "bun run src/seed.ts"
  },
  "dependencies": {
    "elysia": "latest",
    "drizzle-orm": "latest",
    "@elysiajs/cors": "latest",
    "@blackwaves/admingen": "workspace:*",
  "@blackwaves/admingen-adapter-drizzle": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "latest",
    "drizzle-kit": "latest"
  }
}